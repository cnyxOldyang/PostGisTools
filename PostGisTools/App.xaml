<Application x:Class="PostGisTools.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PostGisTools"
             xmlns:vm="clr-namespace:PostGisTools.ViewModels"
             xmlns:v="clr-namespace:PostGisTools.Views"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <DataTemplate DataType="{x:Type vm:ConnectionViewModel}">
            <v:ConnectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SchemaViewModel}">
            <v:SchemaView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DataViewModel}">
            <v:DataView />
        </DataTemplate>
    </Application.Resources>
</Application>
